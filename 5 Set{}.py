#set - must use {}
names = {'maniraj', 'hari', 'kathir'}
#print(names)

#we can add some values in set
names.add('subash')
#print(names)

#dictionary - application of set
#cannot be changed the data
#store date like database
#inside the dictionary - we can insert the 2 fields = "key": "value"

data = {
    "name": "mani",
    "age": "25",
}
print(data.get("age"))
